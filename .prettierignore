# Vendored from the v1 PWA for reference while porting its suggestion engine. Not maintained
# here, and reformatting it would make it harder to diff against the original.
docs/reference/

node_modules/
mobile/.expo/
mobile/android/
mobile/ios/

# Generated by expo-cli on install/prebuild and gitignored, so formatting it is churn that
# only ever shows up as a failing `npm run check` on a fresh checkout.
mobile/expo-env.d.ts
